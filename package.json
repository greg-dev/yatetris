{
  "name": "yatetris",
  "version": "1.0.0",
  "description": "Yet another tetris game",
  "main": "index.js",
  "scripts": {
    "kill-reload": "kill $(ps xo ppid,pid,command | grep '[r]eload-server.js' | awk '{print $1,$2}'); exit 0",
    "reload": "reload -b -s index.html",
    "dev": "npm run kill-reload; npm run reload"
  },
  "author": "Greg",
  "license": "WTFPL",
  "dependencies": {},
  "devDependencies": {
    "semistandard": "^12.0.0"
  }
}
